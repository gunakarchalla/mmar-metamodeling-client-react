import { Box } from "@mui/material";

// Empty stub — mirrors the original right-nav, which only hosted the log window
// (now a sibling region in the layout). Reserved for future right-side content.
export default function RightNav() {
  return <Box className="right-nav" sx={{ flex: "0 0 auto" }} />;
}
